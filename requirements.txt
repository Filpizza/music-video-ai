# Зависимости проекта music-video-ai.
# Установить всё разом:  pip install -r requirements.txt

# --- Веб-интерфейс ---
fastapi==0.139.0        # веб-фреймворк (эндпоинты /api/...)
uvicorn==0.49.0         # сервер, который запускает FastAPI-приложение

# --- Ядро конвейера ---
httpx==0.28.1           # HTTP-запросы к Gemini (brain.py)
python-dotenv==1.2.2    # чтение ключей из .env (config.py)
pillow==11.3.0          # генерация картинок-заглушек (image_generator.py)

# --- Тесты (для разработки) ---
pytest==9.1.1           # запуск тестов: python -m pytest

# Примечание: FFmpeg — это отдельная программа, а не Python-библиотека,
# её нужно установить в систему отдельно (у тебя уже стоит и есть в PATH).
