[pytest]
# Где искать тесты
testpaths = tests
python_files = test_*.py

# Добавляем корень проекта в путь импорта, чтобы тесты видели
# config, brain, main и остальные модули без лишних настроек.
pythonpath = .

# -v = подробный вывод (видно имя каждого теста и прошёл он или нет)
addopts = -v
